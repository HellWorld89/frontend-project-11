extends: airbnb-base
rules:
  linebreak-style: ["error", "unix"]
  indent: ["error", 2]
  no-console: "off"
  semi: ["error", "never"]
  'arrow-parens': ["error", "as-needed"]
  'no-unused-vars': ["error", { "argsIgnorePattern": "^_" }]
env:
  browser: true